import { CustomConfigurationBase } from '@allianz/ngx-aem-integration';

/**
 * Used to Strucutre the AEM data
 */
export interface AEMConfiguration extends CustomConfigurationBase {
  userlastname: string;
}
